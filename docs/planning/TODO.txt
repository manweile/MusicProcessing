NOT DISPOSED:

  Doxygen related:

    1) Doxygen html output
      a) get detailed description to show immediately underneath brief description instead of linked by "More..."
    TODO

    2) find if can have parameters on same line instead of multiple lines
    TODO

    3) regenerate doxyfile - COMPLETED
      a) use custom css - copy from esp scale project - ISSUES, INVESTIGATE
      b) change output directory - COMPLETED
      c) move doxyfile to root - COMPLETED
      d) might need custom xml too, that depends on how dox files uses markdown files, if at all - INVESTIGATE
    IN PROGRESS

    4) index.dox file
      a) move it to root - COMPLETED
      b) think about how to update with contents of various md files
        i) direct use of md files like esp scale?
        ii) find other ways of getting markdown content into dox file
    IN PROGRESS

    7) look into adding GENERATE_REQUIREMENTS tag
    TODO

  Markdown related:

    1) markdown files
      a) consolidate the "directory" markdowns into a workflow.md
      b) finish testing markdown
      c) clean up markdown errors in doxygen_usage.md
        i) pattern it after esp scale doxygen markdown
      d) update doxygen_usage.md
    TODO

  Logic related:

    1) using gc.enable()
      a) Python GC is normally already enabled.
      b) Explicit gc.enable() usually does not prevent ordinary leaks.
      c) Real leaks often come from references, caches, cycles with finalizes, external resources, or native extensions.
    TODO

    2) determine why audio_utilities.py exists
    TODO

    3) add function to update genre metadata
      a) works for all file types in list ie. FLAC (.flac), MP3 (.mp3), MP4 (.m4a), ASF (.wma)
      b) two input options
        i) one off single artist: command line supplying top level directory to start from, artist name, and genre
        ii) multiple artists: command line supplying top level directory to start from and supplying csv file to read artist names and genre from
          1) csv file header: artist name, artist genre
        iii) no matter input source
          1) artist name(s) MUST match artist directory name exactly
          2) only 1 genre per artist name
      b) algorithm:
        i) get top level directory from supplied input
          start walk through in top level directory
          1) for artist name and genre in command line:
              a) looks for all audio files for supplied artist input
              b) sets genre field from supplied input
          2) for csv in command line:
              a) for each line:
                  get artist name
                  get genre
                  looks for all audio files for artist
                  set genre field
        ii) if genre tag exists for metadata type, resets genre field with genre value
        iii) if genre tag does not exist, adds appropriate genre tag for metadata type, populated with genre value
      c) test cases:
        i) will need audio files in \test\Music\
            1) for all 4 types
            2) at least one of 4 files needs empty genre tag
            3) at least one of 4 files needs no genre tag
            4) at least one of 4 files needs genre tag with multi-values
            5) at least one of 4 files needs genre tag with single value
        ii) test files will be copied to test\Music\?? in test_audio_metadata.setUpClass
        iii) will need a test.csv
    TODO

    4) think about changing ebu normalization
      a) to use hybrid loudnorm stats then volume adjust per google session
    TODO

    5) add mode select option to create_txt function
    TODO

    6) rethink has_art_tag logic
      a) why call get_any_tags before checking if input file is FLAC?
    TODO

  Environment related:

    1) determine if I even need templating if I have a python.instructions.md
      a) if yes, determine how they can work together
    TODO

    2) think about moving generated_files from /src to root
    TODO

    3) document external library launch json - why & how to use it
    TODO

    4) Consolidate directories & cleanup
      a) delete unnecessary firs and file
      b) rebuild dir structure
        i) streamline structure
        ii) move files to proper locations
        iii) structure
          /                 root contains only root level files
            .gitignore
            Doxyfile
            doxygen-custom.css
            doxygen.log
            index.dox
            main.py
            workspace file
            README.md
            requirements.txt
          /.github          github instructions and workflows
            /instructions   instructions mds
            /workflows      workflow yamls
          /.vscode          the json files VS code uses
          /docs             information files
            /command_lines  command line examples
            /md_files       info mds
            /excel          spreadsheets
            /extensions     list of extensions in use by vscode
            /launch_configs alternative launch config for code NOT in root (like external libraries)
            /scripts        example & test scripts
              /google       google generated
              /mine         generated by me
          /html             the doxygen
          /src              the python logic files
          /tests            the unit test framework files
          /templating       automated file templates

DISPOSED:

  1) import doxygen create from esp scale project
  COMPLETED

  2) workflow yamls (refer to esp scale yamls)
    a) split current python-app.yaml
      i) build yaml
      ii) test yaml
    b) add an orchestration yaml
    c) add cleanup yamls
  COMPETED

  3) determine why Cream-Badge folder.jpg keeps getting deleted in audio metadata test suite
  Results:
    test_extract_flac_art is extracting Folder.jpg from \tests\Music\Cream\Goodbye\Cream-Badge.flac
    then test_audio-art tearDown is deleting it
    but test_audio_metadata test_convert_file expects the Folder.jpg to exist
  Action:
    a) Find another flac file with embedded art (other than Cream)
    b) extract the art to a Folder.jpg
    c) save flac file and Folder.jpg to tests\Music\artist\album\
    d) add constant for new path to tests\__init__.py and add to __all_ list
    e) import new constant in test_audio_art.py
    f) use new constant instead of TEST_FLAC_CREAM_BADGE
    g) change cls.flac.jpg to use new artist & album
  COMPLETED

  4) add svg graphs to Doxygen output
  COMPLETED

  5) debug doxygen issues with test docs
  COMPLETED

  6) run python.instructions.md on all files
  COMPLETED